export { Card } from '../../../src/ui/Card';
export { default as React } from 'react';
export const AnnouncementCard = ({ children }: any) => children;
export const WorkdayCard = ({ children }: any) => children;
export const StoreLayoutCard = ({ children }: any) => children;
